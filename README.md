## Building

A C++-23 compiler is required for building.
Tested with MSVC v. 18.6.0 (VS 2026) (Windows), GCC 15.2.0 (Linux), Clang 20.1.8 (Linux).

Clone the repository and build:

`mkdir build && cd build && cmake .. && cmake --build .`


